diff --git a/integrations/llm_client.py b/integrations/llm_client.py
index 0000000..0000000 100644
--- a/integrations/llm_client.py
+++ b/integrations/llm_client.py
@@ -226,13 +226,14 @@ def _call_openai_compatible(
     }
     max_tokens = int(max_output_tokens) if max_output_tokens is not None else 8192
+    temperature = 1 if "api.kimi.com/coding" in base_url or model == "kimi-for-coding" else 0.4
     payload = {
         "model": model,
         "messages": [
             {"role": "system", "content": system_prompt},
             {"role": "user", "content": user_message},
         ],
         "max_tokens": max(256, max_tokens),
-        "temperature": 0.4,
+        "temperature": temperature,
     }
     resp = requests.post(url, headers=headers, json=payload, timeout=timeout)
     if resp.status_code != 200:

